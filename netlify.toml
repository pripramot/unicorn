[build]
  command = "pnpm install --frozen-lockfile && pnpm build"
  publish = "apps/web/build"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.33.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
